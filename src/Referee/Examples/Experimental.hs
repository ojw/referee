module Experimental where

-- borrow commands per round w/ rps semantics
-- card or miniature game, w/ per round commands
-- keep core mechanics simple enough for rps to be important
-- probably 3 (or 5 or similar) types of commands, but many individual commands
